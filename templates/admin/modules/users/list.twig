{% extends 'admin/layout/layout.html.twig' %}
{% block header_title %}
    Utilisateurs
{% endblock %}

{% block content %}
      <div class="row mb-2">
            <div class="col-sm-2">
                <div class="card">
                    <div class="text-right">
                        <h3 class="text-dark m-t-5">
{#                                {% set diff = countUsersOfMonth - countUsersOfPreviousMonth %}#}
{#                                {% set diffClass = 'diff-minus' %}#}
{#                                {% if diff > 0 %}#}
{#                                    {% set diffClass = 'diff-plus' %}#}
{#                                {% endif %}#}
{#                                <b class="{{ diffClass}}">({% if diff >0 %}+{% endif %}{{ diff }})</b>#}
                                <b class="counter">{{ countUsersOfMonth }}</b>
                        </h3>
                        <p class="text-muted mb-0">Nouveaux ce mois</p>
                    </div>
                </div>
            </div>
          <div class="col-sm-2">
              <div class="card">
                  <div class="text-right">
                      <h3 class="text-dark m-t-5"><b class="counter">{{ countUsersOfYear }}</b></h3>
                      <p class="text-muted mb-0">Nouveaux cette année</p>
                  </div>
              </div>
          </div>
          <div class="col-sm-2">
              <div class="card">
                  <div class="text-right">
                      <h3 class="text-dark m-t-5"><b class="counter">{{ countUsers }}</b></h3>
                      <p class="text-muted mb-0">Total</p>
                  </div>
              </div>
          </div>
          <div class="col-sm-6 text-right">
              <a href="{{ path('admin_user_export_csv') }}" class="btn btn-success mt-3">
                  <i class="mdi mdi-download"></i> Exporter en CSV
              </a>
          </div>
          <div class="col-md-12">
              <div id="admin-user-list">

              </div>
          </div>
      </div>
{% endblock %}

{% block part_js %}

{% endblock %}